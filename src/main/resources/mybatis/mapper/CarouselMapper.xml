<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.gdufs.mapper.CarouselMapper">

    <insert id="insertCarousel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO carousel(content, path, priority, create_user, update_user)
        VALUES (#{content}, #{path}, #{priority}, #{createUser}, #{updateUser})
    </insert>

    <update id="updateCarousel">
        UPDATE carousel
        SET content = #{content}, path = #{path}, priority = #{priority}, update_user = #{updateUser}
        WHERE id = #{id}
    </update>
    
</mapper>